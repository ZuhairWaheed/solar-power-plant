server:
  port: 8000

spring:
  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/solar_power
    username: base
    password: base-secret
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true